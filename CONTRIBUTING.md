# 知域维护规范

本项目是独立的静态论文仓库。持久化更新必须通过仓库文件和 Git commit 完成；网页中的搜索、筛选、隐藏和评论不会直接修改资源。

## 检索与证据

- 论文线索可以来自 arXiv、Google Scholar、Hugging Face Papers Daily、papers.cool 或社区分享，但二手信息不能替代原始资料。
- 优先核对论文原文、正式出版页、官方项目页和官方代码仓库，并识别预印本、会议版和期刊版之间的关系。
- 尽可能阅读正文。若只能取得标题、摘要或二手介绍，必须说明信息范围和置信度，不得声称已阅读全文。
- 严格区分作者明确声称、实验实际支持和阅读者分析。论文没有证明的泛化、因果或 SOTA 结论不得扩大表述。
- 阅读实验时核对数据集及划分、指标、baseline、公平比较、消融、统计波动、计算成本和潜在数据泄漏。

## 单篇论文内容

每篇论文应尽量包含：

1. 标题、作者、年份、发表状态、arXiv ID 和 DOI。
2. 论文、正式出版、项目、代码、数据、模型和 AlphaXiv 的可靠链接。
3. 能稳定支持检索和分组的类别标签。
4. **当前挑战**：这篇工作面对的具体缺口，而不是领域套话。
5. **研究动机**：作者为何做这项工作，以及核心 Insight。
6. **技术方案**：输入、主要过程、输出，以及与最近 baseline 的实质差异。
7. **实验结果**：设置、主结果、消融，以及证据实际支持的结论。
8. **总结讨论**：贡献收束、适用边界、失败案例与阅读判断。
9. 代码和数据的开放状态。
10. 局限、失败案例和尚未解决的问题（条目化要点）。

代表图仅在论文名称、图号和**可直接打开的原图文件**均可核验时加入。来源必须指向 png / jpg / webp / gif 直链（例如 arXiv HTML 里的 `.../teaser.png`），不要只链到论文 HTML 页面。无法取得原图时不要强行配图。

旧报告中的「核心内容与 Insight」「Pipeline」「实验与证据」「局限」仍然有效。导入器会把新小节写入可选字段，并回填对应的旧字段，所以按新模板入库的论文可以在旧页面上显示，已有论文也不会因为缺少新字段而报错或空白崩溃。

## 报告格式

报告写入 `content/reports/`，文件名建议使用 `YYYY-MM-DD_topic_slug.md`。

- 整份报告使用一个一级标题。
- 在文件开头声明 `**报告标签**：标签1, 标签2`；导入器会将这些标签应用到报告及其论文条目。
- 每篇论文使用连续编号的二级标题，例如 `## 1. Paper Title`。
- 论文条目使用下面的小节标题；**不要改名**，导入器按标题切分。不要把「讨论相关工作」「方法概述」等变体写成独立三级标题。
- 专题报告先写分类比较、研究脉络、证据审计和开放问题，再将连续编号的论文条目放在末尾，避免导入边界含混。

### 论文条目模板

小节标题必须原样使用。技术方案里的 **输入 / 过程 / 输出** 必须单独成行，否则 Pipeline 条带无法填写。

```markdown
## 1. Paper Title

**作者：** First Author, Second Author
**年份与发表：** 2026，会议或 arXiv preprint
**可靠入口：** [arXiv](https://arxiv.org/abs/XXXX.XXXXX)｜[项目](https://example.com)｜[代码](https://github.com/...)
**代表图：** Paper Name，Fig. N，一句话说明。来源：[Fig. N 原图](https://arxiv.org/html/XXXX.XXXXXv1/figure.png)

![Paper Name Fig. N](https://arxiv.org/html/XXXX.XXXXXv1/figure.png)

### 当前挑战

写清现有方法卡住的具体失败模式、过强假设或评测缺口。区分领域长期挑战和本文针对的缺口。不要只复述摘要第一句。信息不足时标明“仅据摘要”。

### 研究动机

写作者为什么做这项工作，核心 Insight 是什么，挑战如何导向方法选择（接口、表征、训练信号或因果/物理假设）。点明与本知识库课题的相关性；若只是相邻工作，写明“间接相关”及可借鉴点。不要再单独写 Insight 小节。

### 技术方案

- **输入：** 模型实际接收的观测、动作、指令或其他条件
- **过程：** 关键模块与训练/推理步骤，突出与最近 baseline 的实质差异
- **输出：** 推理期真正产出的状态、动作、视频或其他结果

理论或框架论文改为形式化接口，并标明无实现。冒号可以写在加粗记号内或外，但输入/过程/输出必须单独成行且同行有内容。不要再单独写 Pipeline 小节。

### 实验结果

覆盖数据集及划分、指标、baseline、主结果、消融或失败设置、计算成本（若论文提供）。明确区分作者主张和实验实际支持的结论。无实验则写“无实证实验”及原因，禁止编造数字。

### 总结讨论

收束贡献、适用边界、失败案例、与相关工作的真正差异、开放问题，以及阅读判断（可引用何处、不可扩大解释何处）。不得把相关性写成因果性。

### 代码与数据

代码/权重/数据是否公开、许可证能否核对、完整训练语料能否复现。

### 局限、失败案例与开放问题

用 3–6 条要点列出；可与总结讨论互补，但不要只写“见上文”。
```

兼容识别：`核心概括 / Insight`、`Pipeline`、`实验与证据` / `实验概括`、`开放情况与局限` 仍可导入。新报告不要混用两套标题，以免切分重复。

## 构建与校验

在仓库根目录运行：

```bash
python3 scripts/ingest_reports.py
python3 scripts/enrich_arxiv.py
python3 scripts/validate_data.py
python3 scripts/check_asset_budget.py
```

导入器按 arXiv ID、DOI、规范化标题去重。未改动的报告再次导入时不会重写已有论文正文。`enrich_arxiv.py` 需要网络；网络不可用时应记录该项未完成，并继续执行其余本地校验。它只补全空标题，不会用 API 题名覆盖已写入的标题。

提交前检查 `git diff`，确认没有重复记录、无效链接、意外删除或无关格式变动。只暂存本次相关的报告、生成数据和必要图片；不要覆盖工作区中他人的修改，也不要在未经授权时执行 `git push`。
